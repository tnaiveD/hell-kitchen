#version 330 core

layout(location = 0) in vec3 inPos;
layout(location = 1) in vec2 inTex;

out vec2 vTex;

uniform mat4 vuModel;
uniform mat4 vuView;
uniform mat4 vuPerspective;

void main(){
	gl_Position = vuPerspective * vuView * vuModel * vec4(inPos, 1.0);
	vTex = inTex;
}