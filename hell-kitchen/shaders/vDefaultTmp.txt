#version 420 core

out vec2 vTex;


layout(location = 0) in vec3 inPos;
layout(location = 1) in vec2 inTex;

// ADVANCED
// ------------------------------

//VS_OUT {
//	type val;
//
//} vs_out;

layout(std140, binding = 0) uniform Matrices
{
	mat4 vuBProjection;
	mat4 vuBView;
};

uniform mat4 vuProjection;
uniform mat4 vuView;
uniform mat4 vuModel;

void main(){

	vTex = inTex;
	gl_Position = vuBProjection * vuBView * vuModel * vec4(inPos, 1.0);


}