#version 330 core

in vec2 vTex;

out vec4 fColor;

uniform sampler2D frame;

void main(){
	if ((texture(frame, vTex).r > 0.5) && (texture(frame, vTex).g > 0.5) && (texture(frame, vTex).b > 0.5))
		fColor = vec4(0.8, 0.8, 0.7, 1.0);
	else
		fColor = texture(frame, vTex);
}